# Evidencia P0-02 — 2026-07-14

## Veredicto

P0-02 queda verificado únicamente para paper trading y preparación offline. No
habilita dinero real, no demuestra rentabilidad y no cierra P0-05
single-writer/fencing ni P0-07 crash/fault injection. Durante toda la
verificación, `configs/risk.yml` mantuvo `live_trading_allowed: false`.

Base Git de referencia: `4ad94269a581d43ce897f953f849fea51cea5919`, con
cambios de working tree todavía sin commit. Los hashes siguientes identifican
el contenido exacto auditado:

```text
d3c01ffb16c51f62f56647e51f84040755b4beffc05f04cd870e9b23c6dfea54  src/trading_ai/execution/order_journal.py
9ed539811d740d1c53dbd43a22c8d2843f1f4b8c91227400e0ad38047496ae33  src/trading_ai/execution/alpaca_paper.py
ce30414ab074871d66429d0dd5ca6cd2f7c64856303eb8a3205d0c30eee5cdf3  src/trading_ai/execution/paper_safe_flatten.py
a18981ad18e4092a2b95f09051f220366cefa21d5203abf8db7f87e493c93d35  src/trading_ai/execution/paper_position_watch.py
14cfbd24845371065870f3ef82660be5f7bd9332165b95fbe3ec2deb0af8c498  src/trading_ai/execution/sleeve_rebalance.py
```

## Resultados reproducibles

Suite completa:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src \
  .venv312/bin/python -m unittest discover -s tests -q
```

Resultado: `Ran 1533 tests in 118.324s` y `OK`.

Pruebas dirigidas ejecutadas por el supervisor: 252/252:

- journal, safe-flatten y paper-daily: 52/52;
- adapter, retry/idempotencia, reduce-only y position-watch: 80/80;
- rebalance, circuit breaker, revalidación y sleeve position-watch: 120/120.

Reauditoría independiente read-only: 223/223. Reprodujo y confirmó:

- un journal ambiguo con bróker 0/0 devuelve `ERROR`, conserva el latch y no
  declara `FLAT`;
- el kill switch queda durable antes de la primera lectura del bróker;
- una identidad de orden broker divergente se bloquea aunque el estado coincida;
- `replaced` se bloquea mientras no exista seguimiento validado de la cadena;
- borrar o sustituir el SQLite durante flatten devuelve `ERROR` y no resetea;
- un fallo de evidencia posterior al reset vuelve a activar el latch;
- risk state ausente o corrupto bloquea; un estado inicial válido puede
  latchearse y resetearse solo tras evidencia 0/0;
- un terminal histórico `REJECTED` se reconcilia con snapshot completo 0/0;
- excepciones, estados y razones externas no filtran secretos.

Verificaciones estáticas:

```text
ruff E9,F63,F7,F82 sobre src/trading_ai y tests: PASS
compileall de src/trading_ai y tests con pycache en /tmp: PASS
git diff --check: PASS
```

## Riesgo residual

- P0-05 debe imponer una única autoridad de escritura y fencing por cuenta.
- P0-07 debe ejecutar fault injection de crash, disco, SQLite, red y bróker.
- Paper trading no modela por completo impacto, latencia, cola, liquidez ni
  costes reales; estos resultados no constituyen evidencia económica.
- Todo live sigue bloqueado hasta cerrar todos los P0 y la campaña P1 con una
  aprobación financiera independiente.
